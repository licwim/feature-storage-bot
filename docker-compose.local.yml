version: '3.3'
services:
  fsb-app:
#    image: licwim/feature-storage-bot:master
    build: .
    volumes:
      - ./:/var/app
      - /var/app/.venv/
    env_file:
      - .env

  fsb-db:
    image: mysql:8.0.32
    volumes:
      - fsb-db:/var/lib/mysql
    ports:
      - "3306:3306"
    env_file:
      - .env

volumes:
  fsb-db: {}